package fr.eni.tp.archenoe;

public class Arche {
    //tableau d'animaux max 8
    private Animal[] tabAnimaux = new Animal[8];

    //méthode pour ajouter un animal
    public Animal ajouter(Animal animal){
        //regarder dans le tableau si il reste de la place
        for (int i = 0; i < tabAnimaux.length; i++) {
            if (tabAnimaux[i] != null) {
                Animal animal = new Animal() {
                }
            }
        }

    }
}
